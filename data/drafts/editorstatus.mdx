---
title: EditorStatus
date: "2025-11-21"
draft: false
summary: how to verigjfgfer tydgahsFBsadfnaklgfuiwiqewyDCFGFDHSFDVXNXVBASDIWQRWEYRIAKjhfbmnbj,hjgHABFAD
authors: ["Bruceleo"]
status: draft
createdAt: "2025-11-21T05:37:17.721Z"
updatedAt: "2025-11-21T05:37:17.722Z"
---

dsApplication:      HTTP (GET, POST, headers, cookies, JSON, etc.)
Security layer:   TLS (encryption, integrity, auth)
Transport:        TCP (reliable connection, 3-way handshake)
Network:          IP
Link:             Ethernet/Wi-Fi/etc.
So: HTTP doesn’t know encryption.
TLS wraps HTTP → that combo is what we call HTTPS.Application:      HTTP (GET, POST, headers, cookies, JSON, etc.)
Security layer:   TLS (encryption, integrity, auth)
Transport:        TCP (reliable connection, 3-way handshake)
Network:          IP
Link:             Ethernet/Wi-Fi/etc.
So: HTTP doesn’t know encryption.
TLS wraps HTTP → that combo is what we call HTTPS.Application:      HTTP (GET, POST, headers, cookies, JSON, etc.)
Security layer:   TLS (encryption, integrity, auth)
Transport:        TCP (reliable connection, 3-way handshake)
Network:          IP
Link:             Ethernet/Wi-Fi/etc.
So: HTTP doesn’t know encryption.
TLS wraps HTTP → that combo is what we call HTTPS.Application:      HTTP (GET, POST, headers, cookies, JSON, etc.)
Security layer:   TLS (encryption, integrity, auth)
Transport:        TCP (reliable connection, 3-way handshake)
Network:          IP
Link:             Ethernet/Wi-Fi/etc.
So: HTTP doesn’t know encryption.
TLS wraps HTTP → that combo is what we call HTTPS.
